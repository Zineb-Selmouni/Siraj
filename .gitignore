node_modules/
dist/
.env
.env.local
.env.*.local
*.local
.DS_Store
.vite/
tsconfig.tsbuildinfo

# La charte et les decks sont des documents INTERNES. Les notes
# d'intervention des .pptx ne sont pas destinées à un dépôt relié à un
# hébergeur. src/styles/tokens.css est versionné à leur place — c'est lui
# que le build consomme (voir scripts/build-tokens.mjs).
Context/

# Livraison de design du logo : SVG sources, PNG, et le fichier
# d'animation (3 Mo de bundle React/Babel). Même règle que la charte —
# src/components/logo/marks.ts est versionné à sa place, c'est lui que
# le build consomme (voir scripts/build-logo.mjs).
# Livraison de design : signes, PNG, films, logo de l'éditeur. Les
# fichiers générés (marks.ts, harmony.ts, public/fonts) sont versionnés
# à leur place — le build ne dépend pas d'un dossier de livraison.
brand/

# Arbitrages de contenu : cite des notes d'intervention internes du deck
# (dont la mise en garde sur le LLM externe). Hors dépôt.
NOTES-INTERNES.md

# Netlify
.netlify/
